#include <stdio.h>
#include <stdlib.h>
#include <memory>

#include "logger.h"


using namespace std;

int main (int argc, char* argv[])
{
    bool success = true;


    exit( (success ? EXIT_SUCCESS : EXIT_FAILURE));
}
